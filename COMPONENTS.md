# 📦 Документация компонентов VentClima Landing

## Обзор архитектуры

Проект построен на компонентном подходе React. Каждая секция сайта - отдельный компонент.

## 🧩 Список компонентов

### 1. Navbar (Навигация)
**Путь:** `src/components/Navbar/Navbar.jsx`

**Особенности:**
- Фиксированная позиция при скролле
- Меняет цвет фона при прокрутке
- Адаптивное мобильное меню (гамбургер)
- Плавная навигация по якорям

**Props:** Нет

**Состояние:**
- `isOpen` - состояние мобильного меню
- `scrolled` - прокручена ли страница

---

### 2. Hero (Главная секция)
**Путь:** `src/components/Hero/Hero.jsx`

**Особенности:**
- Полноэкранная секция
- Анимированный фон с blur эффектами
- Градиентный фон
- Кнопки призыва к действию
- Индикатор прокрутки вниз

**Анимации:**
- Fade in + slide up для текста
- Пульсирующие фоновые круги
- Анимированная стрелка вниз

---

### 3. Services (Услуги)
**Путь:** `src/components/Services/Services.jsx`

**Особенности:**
- Grid layout (2 колонки на desktop)
- 4 карточки услуг
- Иконки из lucide-react
- Hover эффекты (поднятие карточки)
- Градиентные фоны на hover

**Данные:**
```js
services = [
  {
    icon: Component,
    title: string,
    description: string,
    color: string, // Tailwind gradient
  }
]
```

---

### 4. Problems (Проблемы и решения)
**Путь:** `src/components/Problems/Problems.jsx`

**Особенности:**
- 6 пар проблема-решение
- Иконки AlertCircle (проблема) и CheckCircle (решение)
- Двухколоночная layout на desktop
- Поэтапная анимация появления

**Структура:**
```js
problems = [
  {
    problem: string,
    solution: string,
  }
]
```

---

### 5. Benefits (Преимущества)
**Путь:** `src/components/Benefits/Benefits.jsx`

**Особенности:**
- Темный фон с градиентом
- 6 преимуществ в grid layout
- Секция статистики внизу (4 показателя)
- Backdrop blur эффект на карточках

**Данные:**
```js
benefits = [
  {
    icon: Component,
    title: string,
    description: string,
  }
]

stats = [
  { value: string, label: string }
]
```

---

### 6. Testimonials (Отзывы)
**Путь:** `src/components/Testimonials/Testimonials.jsx`

**Особенности:**
- 6 отзывов клиентов
- 3 колонки на desktop
- Рейтинг звездами (5/5)
- Quote иконка как декор
- Hover эффект (поднятие)

**Структура отзыва:**
```js
testimonial = {
  name: string,
  position: string,
  text: string,
  rating: number, // 1-5
}
```

---

### 7. FAQ (Вопросы-ответы)
**Путь:** `src/components/FAQ/FAQ.jsx`

**Особенности:**
- 6 популярных вопросов
- Аккордеон (открывается один вопрос)
- Анимированная иконка ChevronDown
- AnimatePresence для плавности

**Состояние:**
- `openIndex` - индекс открытого вопроса

**Данные:**
```js
faqs = [
  {
    question: string,
    answer: string,
  }
]
```

---

### 8. Contact (Контакты)
**Путь:** `src/components/Contact/Contact.jsx`

**Особенности:**
- Форма обратной связи (4 поля)
- Контактная информация справа
- Валидация полей (required)
- Success сообщение после отправки
- Demo режим (имитация отправки)

**Поля формы:**
- Имя (обязательно)
- Телефон (обязательно)
- Email (опционально)
- Сообщение (опционально)

**Состояние:**
```js
formData = { name, phone, email, message }
isSubmitting = boolean
submitMessage = string
```

**Контакты:**
```js
contactInfo = [
  { icon, title, content, link }
]
```

---

### 9. Footer (Подвал)
**Путь:** `src/components/Footer/Footer.jsx`

**Особенности:**
- 4 колонки информации
- Логотип компании
- Соцсети (Facebook, Instagram, LinkedIn)
- Быстрые ссылки
- Список услуг
- Контактная информация
- Copyright с текущим годом

---

### 10. ScrollProgress (Прогресс прокрутки)
**Путь:** `src/components/ScrollProgress/ScrollProgress.jsx`

**Особенности:**
- Фиксированная полоса сверху
- Отслеживает прогресс прокрутки (0-100%)
- Плавная анимация (spring physics)
- Цвет accent

**Hooks:**
- `useScroll` - от Framer Motion
- `useSpring` - для плавности

---

### 11. ScrollToTop (Кнопка наверх)
**Путь:** `src/components/ScrollToTop/ScrollToTop.jsx`

**Особенности:**
- Появляется после прокрутки 300px
- Фиксированная позиция (bottom-right)
- Иконка ArrowUp
- Smooth scroll наверх
- Анимация появления/исчезновения

**Состояние:**
- `isVisible` - видимость кнопки

---

## 🎨 Общие паттерны

### Анимации (Framer Motion)

**Fade in + Slide up:**
```jsx
initial={{ opacity: 0, y: 30 }}
animate={{ opacity: 1, y: 0 }}
transition={{ duration: 0.6 }}
```

**Stagger эффект:**
```jsx
variants={containerVariants}
transition={{ staggerChildren: 0.2 }}
```

**Intersection Observer:**
```jsx
const ref = useRef(null);
const isInView = useInView(ref, { once: true, amount: 0.2 });
animate={isInView ? "visible" : "hidden"}
```

### Responsive Design

**Breakpoints (Tailwind):**
- `md:` - 768px+
- `lg:` - 1024px+

**Типичный grid:**
```jsx
grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8
```

### Цвета

**Используемые:**
- `primary` - #1e306e (темно-синий)
- `primary-dark` - #162449
- `accent` - #FFC527 (желтый)
- `blue-custom` - #4A9FD8

**Применение:**
```jsx
bg-primary text-white
hover:bg-accent
text-primary
```

## 🔧 Кастомизация

### Добавление новой секции

1. Создайте папку:
```bash
src/components/NewSection/
```

2. Создайте компонент:
```jsx
// NewSection.jsx
import { motion } from 'framer-motion';
import { useInView } from 'framer-motion';
import { useRef } from 'react';

const NewSection = () => {
  const ref = useRef(null);
  const isInView = useInView(ref, { once: true });

  return (
    <section id="new-section" className="py-20" ref={ref}>
      <div className="container mx-auto px-6">
        {/* Контент */}
      </div>
    </section>
  );
};

export default NewSection;
```

3. Добавьте в App.jsx:
```jsx
import NewSection from './components/NewSection/NewSection';

// В return:
<NewSection />
```

### Изменение контента

Все данные хранятся локально в компонентах в виде массивов.
Например, для изменения услуг - отредактируйте массив `services` в `Services.jsx`.

### Добавление новых иконок

```jsx
import { IconName } from 'lucide-react';
<IconName className="w-6 h-6" />
```

Каталог: [lucide.dev](https://lucide.dev)

## 📊 Производительность

### Оптимизации:
- ✅ Intersection Observer (рендер только видимых)
- ✅ `once: true` для анимаций (не повторяются)
- ✅ CSS transform вместо margin/padding
- ✅ Lazy load изображений
- ✅ Code splitting готов (Vite)

### Bundle size:
- React: ~45KB
- Framer Motion: ~30KB
- Lucide Icons: ~5KB (tree-shaking)
- Tailwind: ~10KB (purged)

**Total:** ~90KB gzipped

## 🧪 Тестирование

### Ручное тестирование:

1. **Desktop:**
   - Chrome DevTools (F12)
   - Различные разрешения
   - Lighthouse audit

2. **Mobile:**
   - iOS Safari
   - Android Chrome
   - Device mode в DevTools

3. **Проверка:**
   - [ ] Все анимации работают
   - [ ] Навигация функционирует
   - [ ] Форма отправляется
   - [ ] Нет console errors
   - [ ] Адаптив на всех экранах

## 📝 Лучшие практики

1. **Всегда используйте `useInView`** для секций с анимациями
2. **Добавляйте `id`** к секциям для навигации
3. **Используйте семантические теги** (section, article, nav)
4. **Оптимизируйте изображения** перед добавлением
5. **Тестируйте на реальных устройствах**

## 🔗 Полезные ссылки

- [Framer Motion Docs](https://www.framer.com/motion/)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Lucide Icons](https://lucide.dev)
- [React Docs](https://react.dev)
