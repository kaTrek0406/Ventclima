# 🚀 Деплой на GitHub Pages

## Автоматический деплой через GitHub Actions (Рекомендуется)

### Шаг 1: Включите GitHub Pages в настройках репозитория

1. Перейдите в ваш репозиторий на GitHub: https://github.com/kaTrek0406/Ventclima
2. Откройте **Settings** (Настройки)
3. В левом меню найдите **Pages**
4. В разделе **Build and deployment**:
   - **Source**: выберите **GitHub Actions**
5. Сохраните изменения

### Шаг 2: Запушьте изменения

```bash
git add .
git commit -m "Configure GitHub Pages deployment"
git push origin main
```

После пуша автоматически запустится GitHub Action, который:
- Установит зависимости
- Соберет проект
- Задеплоит на GitHub Pages

### Шаг 3: Проверьте деплой

1. Перейдите во вкладку **Actions** в вашем репозитории
2. Дождитесь завершения workflow (зеленая галочка)
3. Ваш сайт будет доступен по адресу:
   ```
   https://katrek0406.github.io/Ventclima/
   ```

---

## Ручной деплой (Альтернатива)

Если хотите задеплоить вручную:

```bash
npm run deploy
```

Эта команда:
1. Соберет проект (`npm run build`)
2. Задеплоит папку `dist` в ветку `gh-pages`

### После ручного деплоя:

1. Перейдите в **Settings** → **Pages**
2. В **Source** выберите:
   - **Branch**: `gh-pages`
   - **Folder**: `/ (root)`
3. Нажмите **Save**

---

## Важные файлы для деплоя

### ✅ vite.config.js
```javascript
base: '/Ventclima/', // Название вашего репозитория
```

### ✅ package.json
```json
"scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d dist"
}
```

### ✅ .github/workflows/deploy.yml
Автоматический деплой при каждом пуше в main/master

---

## Проверка работы

После успешного деплоя проверьте:

- ✅ Главная страница загружается
- ✅ Видео проигрывается
- ✅ Все изображения отображаются
- ✅ Навигация работает
- ✅ Анимации плавные
- ✅ Форма отправляется

---

## Возможные проблемы

### 404 ошибка после деплоя

**Причина**: Неправильный `base` в vite.config.js

**Решение**: Убедитесь, что `base: '/Ventclima/'` (с большой буквы!)

### Изображения не загружаются

**Причина**: Неправильные пути к файлам

**Решение**: Используйте относительные пути, начинающиеся с `/`:
```jsx
<img src="/logo-venclima.png" />
<video src="/main.mp4" />
```

### Видео не воспроизводится

**Причина**: Файл `main.mp4` не в папке `public`

**Решение**: Убедитесь, что файл находится в `public/main.mp4`

---

## Обновление сайта

При любых изменениях в коде:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

GitHub Actions автоматически пересоберет и задеплоит сайт через 2-3 минуты.

---

## Полезные ссылки

- 🔗 Ваш сайт: https://katrek0406.github.io/Ventclima/
- 📊 GitHub Actions: https://github.com/kaTrek0406/Ventclima/actions
- ⚙️ Настройки Pages: https://github.com/kaTrek0406/Ventclima/settings/pages
- 📦 Репозиторий: https://github.com/kaTrek0406/Ventclima

---

## Готово! 🎉

Ваш сайт VentClima готов к деплою на GitHub Pages!
